# Audit Frontend Knowledge

Audit frontend shows who did what, when, against which resource, and why.

Must support safe search/filtering when backend exposes it.

Do not expose sensitive token, credential, or raw personal data unless explicitly required and authorized.
