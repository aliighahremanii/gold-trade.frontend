services:
  frontend:
    image: registry.example.com/trade/frontend:latest
    restart: unless-stopped
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_APP_ENV: production
      NEXT_PUBLIC_API_BASE_URL: https://api.example.com
    networks:
      - web

networks:
  web:
    external: true
